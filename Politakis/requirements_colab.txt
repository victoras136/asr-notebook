# ── System dependencies (run BEFORE pip) ──────────────────────
# !apt-get install -y ffmpeg espeak-ng

# ── Core ML ───────────────────────────────────────────────────
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24.0,<2.0
scipy>=1.11.0

# ── ASR ───────────────────────────────────────────────────────
faster-whisper>=1.0.0
transformers>=4.45.0
accelerate>=1.0.0

# ── Diarization ───────────────────────────────────────────────
pyannote.audio>=3.1.1

# ── Audio Processing ──────────────────────────────────────────
pydub>=0.25.1
soundfile>=0.12.0

# ── LLM ───────────────────────────────────────────────────────
openai>=1.30.0

# ── NLP / Evaluation ──────────────────────────────────────────
rouge-score>=0.1.2
jiwer>=3.0.0

# ── Monitoring ────────────────────────────────────────────────
psutil>=5.9.0

# ── Google Drive (Colab has google-colab built-in) ────────────
google-api-python-client>=2.0.0

# ── Colab fixes ────────────────────────────────────────────────
nest_asyncio>=1.5.0       # asyncio.run() inside Colab event loop
torchvision>=0.16.0       # pyannote needs torchvision::nms

# ── TTS Models ────────────────────────────────────────────────
# Kokoro:     pip install kokoro>=0.9.4        (Apache 2.0)
# Dia-1.6B:   pip install git+https://github.com/nari-labs/dia.git   (Apache 2.0)
# Bark:       pip install git+https://github.com/suno-ai/bark.git     (MIT)
# XTTS-v2:   pip install git+https://github.com/coqui-ai/TTS.git  (CPML — academic OK)
# F5-TTS:     pip install git+https://github.com/SWivid/F5-TTS.git    (MIT)

kokoro>=0.9.4

# NOTE: Do NOT 'pip install bark' — that's an unrelated work-diary package.
# NOTE: Do NOT 'pip install dia' — that's also an unrelated work-diary package.
# NOTE: TTS is installed from git in Cell 2 (coqui-ai/TTS.git) — NOT from PyPI.
