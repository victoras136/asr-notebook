# UI + Drive bridge — local machine and Docker
# ML deps (torch, transformers, faster-whisper, pyannote) run on Colab, not here.

# ── Streamlit UI ─────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-autorefresh>=1.0.0
pandas>=2.0.0
plotly>=5.0.0

# ── Google Drive bridge ───────────────────────────────────────────────────
google-auth>=2.0.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.0.0

# ── Accuracy metrics (Accuracy Check page) ───────────────────────────────
rouge-score>=0.1.2
jiwer>=3.0.0
sacrebleu>=2.4.0
